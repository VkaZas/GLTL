let LTL = require('algorithm/LTL');
let $ = require('jquery');
let GridWorldPainter = require('gridworld-painter');

let painter;

$(document).ready(() => {
    painter = new GridWorldPainter();

});

